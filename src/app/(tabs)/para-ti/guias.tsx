import { SectionScreen } from "@/components/layout/SectionScreen";

export default function GuidesRoute() {
  return <SectionScreen title="Guías">Las guías todavía no están habilitadas.</SectionScreen>;
}
