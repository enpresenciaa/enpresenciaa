import { Redirect } from "expo-router";

// Ruta de compatibilidad con el prototipo anterior.
export default function InitialEvaluationRoute() {
  return <Redirect href="/onboarding/poder-del-cambio" />;
}
